disconnect -- Command to disconnect from a lightning node

The **disconnect** RPC command disconnects from a connected peer.

- **id** (pubkey): The pubkey of the peer to disconnect
- **force** (bool, optional): Force disconnect even if channel is active
