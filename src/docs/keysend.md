keysend -- Command to send funds to a node without an invoice

The **keysend** RPC command sends funds directly to a node without requiring an invoice.

- **destination** (pubkey): The pubkey of the destination node
- **amount_msat** (string): Amount in millisatoshi
- **label** (string, optional): Label for tracking
