listtransactions -- Command to list transactions from the on-chain wallet

The **listtransactions** RPC command returns transactions from the on-chain wallet.

- **start** (u64, optional): Start index for pagination
- **limit** (u32, optional): Maximum number of entries to return
