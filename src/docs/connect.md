connect -- Command to connect to a lightning node

The **connect** RPC command connects to a remote lightning node.

- **id** (pubkey): The node pubkey, optionally with @host:port
- **host** (string, optional): The host:port if not included in id
