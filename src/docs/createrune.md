createrune -- Command to create a new rune

The **createrune** RPC command creates a new rune (authentication token) for accessing the node.

- **restrictions** (array, optional): Array of restriction strings (e.g. ["method=getinfo"])
- **readonly** (bool, optional): If true, create a read-only rune
