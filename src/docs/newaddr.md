newaddr -- Command to generate a new on-chain address

The **newaddr** RPC command generates a new on-chain address for receiving funds.

- **addresstype** (string, optional): Address type - "bech32" or "p2tr" (default "bech32")
